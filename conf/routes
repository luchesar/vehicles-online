# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET           /                                                                       controllers.change_of_address.P1.present

# Map static resources from the /public folder to the /assets URL path
GET           /assets/*file                                                           controllers.Assets.at(path="/public", file)

# Page 2 Keeper status
GET           /p2keeperstatus                                                         controllers.change_of_address.P2KeeperStatus.present
POST          /p2keeperstatus                                                         controllers.change_of_address.P2KeeperStatus.submit
